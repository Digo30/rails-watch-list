<h1>Movies List</h1>


<%= link_to "New List", new_list_path, class: "btn btn-outline-secondary"%>

<ul>
  <% @lists.each do |list| %>
    <li><%= link_to list.name, list_path(list) %></li>
  <% end %>
</ul>
